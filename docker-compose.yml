version: '3.8'

services:
  la-vitrine:
    build: .
    container_name: la-vitrine
    ports:
      - "8080:8080"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
